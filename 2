#2. Сгенерировать 20 чисел в диапазоне от 1 до 20 и после генерации каждого числа выводить на
#строку из «звездочек», количество которых соответствует сгенерированному числу.

import random

for i in range(20):
    number = random.randint(1, 20)
    stars = "* " * number
    print(stars)
